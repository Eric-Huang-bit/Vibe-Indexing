# scripts/

This directory is for utility and helper scripts that support development,
data processing, or deployment workflows.

Examples of scripts to add here:
- `process_video.py` – extract frames from a video for manual inspection
- `batch_describe.py` – run the description pipeline on a folder of clips
- `evaluate.py` – compute evaluation metrics on generated descriptions
